# 全局回复表达规则

## 构建范围与缓存隔离

- 修改代码前先判断本次属于“只检查”“前端改动”“Rust 后端改动”还是“依赖改动”，只执行该范围需要的步骤，不得把局部修复扩大成完整依赖重建。例如：只修改 Markdown 文案时运行类型检查和相关测试即可；只有需要交付新的桌面程序时才制作 `llm-wiki.exe`。
- Rust 测试必须通过 `scripts/test-llmwiki-rust.ps1` 运行，并固定使用 `D:\llmwiki-deps\cargo-test-target`。严禁让 `cargo test`、测试专用特性或测试构建产物写入桌面构建缓存 `D:\llmwiki-deps\cargo-target`。例如：验证一个 Rust 下载函数时，测试产物进入 `cargo-test-target`，随后制作桌面版时仍能复用原来的桌面缓存。
- `D:\llmwiki-deps\cargo-target` 只允许 Tauri 独立桌面构建使用。不得为了省一条命令让测试和桌面程序共用它，也不得在普通修复中清空该目录。例如：测试启用了不同的功能组合时，共用缓存可能迫使桌面版重新制作 PDF、全文检索和数据处理依赖。
- 一轮修改按“相关快速测试 → 完整验证 → 一次最终构建 → 部署”的顺序执行。测试失败期间不得反复制作桌面程序；只有代码和测试都稳定后才运行一次 `prepare-llmwiki.ps1`。例如：修复导入错误时可以多次运行对应测试，但不能每修一行就生成一次 exe。
- 没有修改对应的依赖锁文件且现有依赖目录完整时，不重新执行 `npm ci` 或重新下载 Rust 依赖。例如：`package-lock.json` 没变时复用 `node_modules`；只有锁文件改变或依赖目录缺失时才重新安装。
- 构建判断以真正参与程序制作的源码和配置内容为准，不以整个仓库的提交号或文件时间作为唯一依据。例如：只修改 `AGENTS.md`、计划或证据文档时，不应重新制作桌面程序；修改 `src-tauri/src` 时必须重新制作。
- 同一时间只允许一个桌面构建。构建开始前必须取得互斥锁，检测到已有构建时立即停止第二个任务，不得并行争用缓存。例如：误点两次准备脚本时，第二次应提示已有任务，而不是再启动一套 Cargo 编译。
- 每次构建必须记录各阶段耗时、缓存是否复用、输入指纹和最终版本；失败时不得覆盖现有可用程序。例如：前端制作失败时，`D:\llmwiki-deps\app\llm-wiki.exe` 必须保持原样，并能从日志看出失败发生在哪一步。
- 修改必须保持外科手术式范围。构建或测试暴露无关问题时先报告，不得顺手重构其他业务管线。例如：修复 MinerU 下载时，不得同时改变 PDF 提取、图片描述或知识生成流程。

- 直白表达不等于简化掉信息。回复必须同时保证容易理解、细节完整和表述精确；不得为了让内容显得简单而省略关键条件、适用范围、版本、数字、单位、时间、路径、命令参数、错误信息、风险、例外或不确定性。应先用日常语言说明结论，再给出支撑结论所需的准确细节。
  - 例如：不能把“该功能仅支持 Windows 11 23H2，并且要求 Node.js 20.11 或更高版本”简化成“Windows 上可以使用”。应保留系统版本和 Node.js 版本要求，并说明版本不满足时功能可能无法启动。

- 回答问题时要覆盖用户作出判断或完成操作所需的信息。既不能只给术语和技术细节，也不能只给笼统结论；事实、依据、影响、操作方法和必要的限制应按当前问题完整说明。无法确认的信息要明确标注，不能用肯定语气猜测。
  - 例如：检查某个程序是否安装时，不能只写“没有安装”，也不能只贴一段命令输出。应说明“当前命令行暂时找不到该程序”，列出已经检查的位置，并说明是否还需要检查其他安装目录或沙箱外环境。

- 所有面向用户的回复都必须使用直白、日常、容易理解的语言。先说结论或实际影响，再补充原因和技术细节。不要默认用户熟悉专业术语。
  - 例如：不要只说“请求因鉴权失败被拒绝”，要说“系统没有确认你的登录身份，所以拒绝了这次请求。常见情况是登录已过期，需要重新登录。”

- 回复中的每一个独立板块都必须包含至少一个具体、直白的例子。板块包括标题下的说明、单独的要点组、方案比较、原因分析、操作步骤和风险提示。例子应紧跟所解释的内容，并优先使用“例如：”明确标出。
  - 例如：在“可能的风险”板块中，不能只写“可能丢失数据”，还要写“例如：如果直接覆盖配置文件，你之前填写的服务器地址可能会消失。”

- 整篇回复必须让不熟悉项目内部实现的人也能看懂。出现技术名词、缩写、命令或抽象概念时，要立即用一句通俗话解释它是什么、会造成什么影响。
  - 例如：提到“缓存”时，要补充“缓存就是系统暂时保存的旧结果；例如你已经更换头像，但页面仍显示旧头像，通常就是缓存还没更新。”

- 不要只描述系统内部发生了什么，还要明确告诉用户这对他意味着什么、接下来能做什么。
  - 例如：不要只写“数据库迁移未执行”，要写“新版本需要的数据表还没有建立，所以保存功能暂时不能用。执行迁移后，系统会把缺少的表补上。”

- 解释步骤时，每一步都要使用具体动作，并在容易误解的地方给出输入或结果示例。
  - 例如：不要只写“修改环境变量”，要写“打开 `.env` 文件，把 `PORT=3000` 改成 `PORT=8080`；保存后，服务会改用 8080 端口启动。”

- 如果一句话仍可能让普通用户产生“这是什么意思”或“然后呢”的疑问，就继续改写或补充例子，直到含义、影响和下一步都清楚。
  - 例如：“构建失败”还不够清楚，应写成“项目没有生成可运行的安装包；例如现在双击 `dist` 里的程序可能打不开。需要先修复报错，再重新执行构建。”

- 简短确认类回复也要保持直白，但不必为了形式拆成多个板块；只需用一句话说明结果，并在有歧义时补一个例子。
  - 例如：“已经改好了：以后每个说明板块都会配一个具体例子，技术词也会马上翻成日常说法。”

## Windows 桌面版：快速本机构建与更新

- “编译/构建”是把 TypeScript 前端和 Rust 后端代码制作成 Windows 可运行程序的过程。普通功能修改优先生成“独立运行版”，不默认生成完整发布安装包；例如只修改导入日志时，生成的 `llm-wiki.exe` 必须自带界面文件，双击后不能依赖 `localhost` 上的临时开发服务器。

- 本机已经有可复用的 Rust 构建缓存，必须优先使用：`D:\llmwiki-deps\cargo-target`。构建时设置 `CARGO_TARGET_DIR=D:\llmwiki-deps\cargo-target`；不要默认使用项目内空的 `src-tauri\target`，因为那会重新编译全文检索、PDF 和桌面依赖，可能耗时十分钟以上。
  - 例如：修改 `src/lib/ingest.ts` 后，使用共享缓存通常只需重新链接程序；使用空缓存则会重新编译数百个底层库。

- Rust 构建前先确认并复用本机已有工具。当前已验证的工具路径为：
  - Cargo：`D:\llmwiki-deps\cargo\bin\cargo.exe`
  - Protocol Buffers 编译器（protoc）：`D:\llmwiki-deps\protobuf\bin\protoc.exe`
  - 构建临时目录：仅对当前命令设置 `TEMP=C:\tmp`、`TMP=C:\tmp`，避免中文用户目录导致 protoc 创建临时文件失败。
  - 例如：不要为了“找不到 protoc”直接安装新工具；先按 `Get-Command`、`where.exe`、实际 `--version` 和上述路径确认，再复用已有程序。

- 普通桌面程序更新顺序：
  1. `npm run typecheck`，确认 TypeScript 没有类型错误；例如改了日志字段但参数类型不匹配，会在这一步发现。
  2. 使用 `D:\llmwiki-deps\prepare-llmwiki.ps1` 构建独立运行版。该脚本会先生成前端资源，再通过 Tauri 的构建命令把它嵌入 exe，并写入 `build-info.txt`。
  3. 确认用户已关闭 LLM Wiki，再由准备脚本更新 `D:\llmwiki-deps\app\llm-wiki.exe`；覆盖前必须创建带日期的备份，且不得删除旧版。
  - 例如：更新前先生成 `D:\llmwiki-deps\app\llm-wiki.before-YYYYMMDD.exe`，更新失败时用户仍可直接恢复旧程序。

- 严禁把单独执行 `cargo build` 得到的 `D:\llmwiki-deps\cargo-target\debug\llm-wiki.exe` 直接复制到 `D:\llmwiki-deps\app\llm-wiki.exe`。它是开发版后端程序，可能会加载 `http://localhost` 的临时界面；只有经过 Tauri 独立构建流程、且 `build-info.txt` 已更新的 exe 才能部署到 app 目录。
  - 例如：如果窗口标题变成 “Tauri App” 并显示 “localhost 拒绝连接”，说明错误部署了开发版，应立即恢复备份，不得要求用户启动本地开发服务器。

- 只有用户明确要求“发布安装包”“正式安装器”或“release 优化版”时才运行完整发布构建。此时应先说明它会慢，并且保留上述独立运行版更新路径作为较快的验证方式。
  - 例如：用户只说“修复后让我试用”，应优先更新独立运行版；用户说“给其他电脑安装”，才构建安装包。

- 构建过程中不要关闭、重启或覆盖正在运行的 `llm-wiki.exe`。每次更新前先只读检查进程；如果程序仍开着，就要求用户关闭后再覆盖。
  - 例如：程序仍在运行时直接替换 exe，Windows 可能拒绝覆盖或留下半更新文件。

## 开发迭代完成后提交并推送

- 每次完成一项开发或迭代，并通过与本次修改相对应的必要检查后，必须及时创建 Git 提交并推送当前分支，不得把多个已经完成的任务长期堆积在本地工作区。
  - 例如：完成“修复导入日志”并通过类型检查后，应立即提交并推送；不要等到“搜索优化”和“界面调整”也完成后，再把三个互不相关的任务混在一次提交里。

- 提交前必须检查实际差异，只纳入本次任务直接涉及的文件，不得擅自提交用户原有或其他任务留下的修改；提交说明要直白写清本次完成了什么。
  - 例如：本次只修改 `src/lib/ingest.ts` 时，不应顺带提交用户尚未完成的 `README.md` 修改；提交说明可以写成 `fix: 修复导入日志缺失`。

- 如果因为远程仓库拒绝、网络不可用、登录失效、分支保护或缺少权限而无法推送，必须保留本地提交，并向用户明确说明失败原因、尚未推送的提交以及下一步操作，不得把“已提交”说成“已推送”。
  - 例如：远程仓库要求先更新分支时，应说明“提交已保存在本地，但远程仓库拒绝接收，所以其他电脑暂时看不到这次修改”，并列出需要先同步还是需要用户处理权限。
