# LLM Wiki 0.6.6：Windows 安装与部署说明

这份说明面向收到 Windows 安装 ZIP 的使用者。安装后可直接打开桌面程序，不需要安装 Git、Rust 或启动开发服务器。例如：解压 ZIP 后运行其中名称以 `-setup.exe` 结尾的安装程序。

## 1. 安装前准备

- 使用 64 位 Windows 10 或 Windows 11（x64）；本包不适用于 macOS、Linux 或 Windows ARM 原生安装。
- 首次安装保持联网。界面依赖 Microsoft Edge WebView2 Runtime；如果系统缺少它，安装程序会尝试下载并安装，因此本包不是完整离线安装包。
- 准备自己使用的模型服务地址、模型名称和 API 密钥（服务提供商发给你的访问凭证）。模型服务可能单独收费；安装程序不包含作者的密钥或个人知识库。例如：程序能打开，但未配置模型时，不能据此判断 AI 生成功能已经可用。

## 2. 安装和启动

1. 将 ZIP 完整解压到一个普通目录，例如 `D:\Downloads\LLM-Wiki`；不要直接在压缩包内运行。
2. 双击 `LLM Wiki_0.6.6_x64-setup.exe`，按安装向导操作。优先选择有足够空间的数据盘，例如 `D:\Apps\LLM-Wiki`；具体是否能选择目录以安装向导为准。
3. 安装完成后从开始菜单打开 LLM Wiki。正常结果是出现完整桌面界面，不需要另开网页服务器。
4. 此分发包未配置商业代码签名，Windows 可能显示“未知发布者”。先向发送者核对 ZIP 来源及 SHA-256 校验值，不要对来源不明的同名程序直接放行。

WebView2 属于 Microsoft 运行组件，其系统安装位置由安装器管理。应用的用户设置也可能保存在 Windows 用户数据目录；选择 D 盘安装应用不代表所有设置都会写入 D 盘。例如：知识库目录可以选 `D:\Knowledge\MyWiki`，但账号设置仍可能保存在当前 Windows 用户目录。

## 3. 第一次使用和验收

1. 打开设置，填写模型服务地址、API 密钥和模型名称，并使用界面提供的连接检查（如有）。例如使用兼容 OpenAI 的服务时，地址和模型名必须以该服务提供商给出的信息为准。
2. 创建一个测试知识库，并选择自己可写的目录，例如 `D:\Knowledge\FirstWiki`。
3. 先导入一小段 Markdown 文字或一页普通文字 PDF，观察活动面板的处理结果，再打开生成的 Wiki 页面。PDF 读取组件已经随安装包提供。
4. 关闭程序再重新打开，确认测试知识库和生成页面仍可访问。完成这一步后再导入大批资料。

本包只安装程序，不会同步发送者的知识库、聊天记录或密钥。例如需要迁移已有知识库时，应由原持有人在程序中导出项目，再由新电脑导入，不能只复制安装程序。

## 4. 可选能力需要额外准备

日常桌面界面与内置 PDF 读取不要求安装 Node.js。若要让其他 AI 工具通过 MCP 连接知识库（即让其他工具调用本机知识库功能），需要另外安装 Node.js 20 或以上，并按应用设置中的连接说明操作。安装包已经带上相应服务代码，但没有附带 Node.js 运行程序。例如：只在 LLM Wiki 窗口里使用时，无需先配置这项连接。

模型调用需要对应的模型服务；扫描 PDF 的文字识别、语音/视频解析等功能还可能需要各自的服务或工具。请按具体功能提示配置。例如：普通文字 PDF 能读取，不表示没有配置文字识别服务的扫描图片 PDF 也能同样处理。

## 5. 更新、卸载与故障处理

- 更新前先在应用中导出重要项目并正常退出，再运行新版安装程序。例如不要在正在导入 PDF 时覆盖安装。
- 如需回退，保留旧安装包和更新前导出的项目。重新安装旧程序不一定能恢复已被新版改写的数据。
- 卸载通过 Windows“设置 → 应用 → 已安装的应用”完成。不要把知识库文件夹当作安装文件夹一起删除；设置与个人知识库是否保留，应在卸载后实际检查。
- 窗口空白：检查或修复 Microsoft WebView2 Runtime，再重开应用。
- 资料生成失败：查看活动面板的具体错误，检查模型地址、密钥、网络和服务余额。例如密钥过期时，安装本身仍然可能成功。
- 出现 `localhost` 无法连接：应核对是否误用了开发版程序；本 ZIP 内应使用 `-setup.exe` 安装程序，无需启动开发服务器。

## 6. 源码与版本对应

源码主分支： https://github.com/richenHe/llm-wiki/tree/main 。本次精确提交号、构建信息与文件校验值见 ZIP 内的版本说明及校验清单。同为 0.6.6 的不同提交可能包含不同改动，应以提交号和校验值辨认。

需要自行修改或从源码部署的人，请阅读仓库的 `README_WINDOWS.md`。那份文档面向开发者；普通接收者按本文件安装即可。例如：安装使用者不需要执行 `npm ci` 或 `cargo build`。

## 7. 验证边界

最终交付消息和 ZIP 内版本说明会记录本次实际完成的检查。编译成功、安装包生成、实际安装启动、模型生成成功是不同的检查，不能相互替代。例如：没有在收件人的电脑和模型账号上测试，就不能保证其代理网络或模型账号配置可用。
